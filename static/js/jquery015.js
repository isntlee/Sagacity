
$(document).ready(function () {;
      $('select').formSelect();
      $('.materialboxed').materialbox();
      });
      
      $(document).ready(function () {
      var selectorvar = $('#selectordiv').html();
      if (window.console) console.log('foo');
        if (selectorvar == 'blue')
        
        { $(".nav-wrapper").removeClass("pink").addClass("transparent");
        }

        });
    
    if (window.console) console.log('foo');

    window.console&&console.log('foo');